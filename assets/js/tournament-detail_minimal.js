// Minimal Tournament Detail JavaScript for testing

// Initialize tournament detail page
document.addEventListener('DOMContentLoaded', function () {

    // Get tournament ID from URL
    window.joinTournament = function() {
        alert('joinTournament function works!');
    };
    
    window.updateTournamentStatus = function(status) {
        alert('updateTournamentStatus function works!');
    };
    
    window.handleLogout = function() {
        alert('handleLogout function works!');
    };
    
});
